using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PanelsTween : MonoBehaviour
{
    // [Header("Button Panels")]
    // [SerializeField] private GameObject RightPanel, TopPanel, BottomPanel;

    void Start()
    {
        // LeanTween.move(TopPanel,TopPanel.transform.position + new Vector3(0f,y: -150f,0f), 1f).setDelay(1f).setEaseInExpo();

        // LeanTween.moveX(RightPanel,RightPanel.transform.position.x + -135f, 1f).setDelay(1f);

        //  LeanTween.moveY(BottomPanel,BottomPanel.transform.position.y + 150f, 1f).setDelay(1f);

    }
}
